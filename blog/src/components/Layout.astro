---
import '../styles/global.css';

export interface Props {
  title: string;
  seoTitle?: string;
  socialTitle?: string;
  description?: string;
  image?: string;
}
const { title, seoTitle, socialTitle, description, image } = Astro.props;
const displayTitle = seoTitle || title;
const ogTitle = socialTitle || title;
const siteUrl = "https://tekeep.com";
const ogImage = image ? (image.startsWith('http') ? image : `${siteUrl}${image}`) : undefined;
const currentUrl = new URL(Astro.url.pathname, siteUrl).href;
---
<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />
    <title>{displayTitle}</title>
    {description && <meta name="description" content={description} />}
    <!-- OGP meta for social sharing -->
    {ogImage && <meta property="og:image" content={ogImage} />}
    <meta property="og:title" content={ogTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={currentUrl} />
    <meta property="og:type" content="article" />
    
    <!-- Twitter Card data -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={ogTitle} />
    <meta name="twitter:description" content={description} />
    {ogImage && <meta name="twitter:image" content={ogImage} />}

    <!-- Google Analytics (本番環境のみ) -->
    {import.meta.env.PROD && (
      <>
        <script is:inline async src="https://www.googletagmanager.com/gtag/js?id=G-Y15FLWQ7X1"></script>
        <script is:inline>
          window.dataLayer = window.dataLayer || [];
          function gtag(){dataLayer.push(arguments);}
          gtag('js', new Date());
          gtag('config', 'G-Y15FLWQ7X1');
        </script>
      </>
    )}
  </head>
  <body>

<slot />
  </body>
</html>
